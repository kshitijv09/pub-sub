# Optional: require X-API-Key header for all REST and WebSocket requests.
# If set, clients must send header: X-API-Key: <value>
API_KEY=my-secret-api-key

# Optional: heartbeat interval in seconds (default 30). Set to 0 to disable.
# HEARTBEAT_INTERVAL_SEC=30

# Optional: max messages per consumer queue (default 1024). Overflow sends SLOW_CONSUMER.
# SUBSCRIBER_QUEUE_MAX_SIZE=1024

# Optional: ring buffer size per topic for replay / last_n (default 100).
# TOPIC_RING_BUFFER_SIZE=100
